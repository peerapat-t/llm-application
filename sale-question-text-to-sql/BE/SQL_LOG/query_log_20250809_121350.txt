SELECT p."ProductName", SUM(o."QuantityOrdered" * p."Price") AS "TotalRevenue"
FROM orders o
JOIN products p ON o."ProductID" = p."ProductID"
GROUP BY p."ProductName"
ORDER BY "TotalRevenue" DESC
LIMIT 5;